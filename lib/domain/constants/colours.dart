import 'package:flutter/material.dart';

/// Colors related to the App.
class AppColors {
  /// Tint1 for primary color. Used for background scaffold or dark
  /// text appearance.
  static const primary = Color(0xFF42A1FF);

  /// Shade1 for primary color. Used for light text appearance and icons.
  static const shade1 = Color(0xFF585858);

  /// Shade2 for primary color.
  static const shade2 = Color(0xFFC7C7C7);

  /// Error color
  static const error = Color(0xFFC43737);

  /// Warning color
  static const warning = Color(0xFFF3E656);

  /// Success color
  static const success = Color(0xFF33AA42);
}